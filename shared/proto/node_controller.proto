syntax = "proto3";

package novacloud;

option go_package = "./proto";

service NodeController {
    rpc RegisterNode(RegisterNodeRequest) returns (Nothing);
    rpc ProvisionResources(ProvisionResourcesRequest) returns (Nothing);
}

message Nothing { }

message RegisterNodeRequest {
    int32 port = 1;
}

message ProvisionResourcesRequest { }
